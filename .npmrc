# Configuração do npm para builds

# Desabilita audit automático (economiza tempo no CI/CD)
audit=false

# Desabilita funding messages
fund=false

# Usa cache local
cache=.npm

# Produção apenas em builds
production=false

# Salva exato (não usa ranges)
save-exact=true

# Loglevel
loglevel=error
